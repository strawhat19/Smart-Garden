export enum Themes {
    dark = `dark`,
    light = `light`,
}

export enum AuthStates {
    signin = `signin`,
    signup = `signup`,
    signout = `signout`,
}

export enum AuthLabels {
    signin = `Sign In`,
    signup = `Sign Up`,
    signout = `Sign Out`,
}