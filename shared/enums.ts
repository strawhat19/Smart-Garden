export enum Themes {
    dark = `dark`,
    light = `light`,
}

export enum AuthStates {
    signin = `signin`,
    signup = `signup`,
}

export enum AuthLabels {
    signin = `Sign In`,
    signup = `Sign Up`,
}